<% if p("rabbitmq.tls.enabled") -%>
<% if_p("rabbitmq.tls.ca") do |cert|
  if !cert.match("CERTIFICATE")
    cert = cert.gsub(/^(.{40}).*/, '\1...')
    raise "rabbitmq.tls.ca '#{cert}' does not look like a TLS certificate"
  end
-%>
<%= p("rabbitmq.tls.ca") %>
<% end -%>
<% end -%>
