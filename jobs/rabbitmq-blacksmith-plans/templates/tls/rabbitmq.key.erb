<% if p("rabbitmq.tls.enabled") -%>
<% if_p("rabbitmq.tls.key") do |key|
  if !key.match("PRIVATE KEY")
    key = key.gsub(/^(.{40}).*/, '\1...')
    raise "rabbitmq.tls.key '#{key}' does not look like a TLS private key"
  end
-%>
<%= p("rabbitmq.tls.key") %>
<% end -%>
<% end -%>
